e2-action action="[action.meta.info[f].assoc, 'list']"
  .col-lg-3.col-md-4.col-sm-5
    .form-group
      .input-group.input-group-sm
        button.btn.btn-default [
          type="button"
          ng-init="info = action.meta.info[f]"
          ng-model="action.selected"
          bs-options="v.id as v.value for v in action.values"
          ng-change="action.change()"
          ng-class="info.render.class"
          class="form-control"

          data-animation="{{::info.render.animation}}"
          data-placeholder="{{::info.loc}}"
          data-multiple="1"
          data-max-length="3"
          data-max-length-html="wybranych"
          data-html="true"
          bs-select=""
          e2-search-field=""
        ]
        .input-group-btn
          a.btn.btn-default ng-disabled="action.selected.length == 0" ng-click="action.reset()"
            span.glyphicon.glyphicon-off
